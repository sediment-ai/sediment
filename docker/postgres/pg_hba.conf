# The supported deployment permits only SCRAM authentication, including local SQL.
# Database network membership and separate role grants provide the other boundaries.
local all all scram-sha-256
host all all 0.0.0.0/0 scram-sha-256
host all all ::/0 scram-sha-256
