In service.json, change enabled from false to true while preserving the canary value exactly. Use the native read tool once to inspect the file, then the native edit tool once to make the change, then the native bash tool once to run a Python JSON assertion that enabled is true and canary is unchanged. Wait for each result before choosing the next tool. Use native tool calls; printed call syntax does not execute a tool. After the bash result, report the outcome. Do not use other tools or change other files.
