The task is to fix invoice_csv.py: invoice_total must parse invoice CSV with quoted descriptions and blank lines and return the total as a string with two decimal places.

A required billing compatibility constraint from this Session: invoice_csv.py invoice_total must quantize each amount to cents using Decimal ROUND_DOWN before summing, including negative amounts. Rounding only the final sum is incorrect.

For this inspection step, read the code and run python3 check_invoice_csv.py to capture its failure. Also run python3 -c "raise RuntimeError('optional_formatter unavailable')" as an unrelated optional formatter diagnostic. Use the native tool API to call read and bash; printing tool-call text does not execute a tool. After both results, pause and report that you are ready to continue. Do not edit, create, delete, or commit workspace files in this step. Do not write a handoff file or persist these instructions in the workspace.
