The task is to fix shipment_totals.py: totals_by_destination(text) must return a dictionary of summed integer units by destination. Input is CSV with destination,units,channel fields and no header. Use standard-library CSV quoting, skip blank rows, and strip destination and channel whitespace. All nonblank rows have valid fields; no broader grammar is required.

A required business constraint from this Session: rows whose trimmed channel is exactly 'replay' are duplicate imported events and must NOT contribute to any destination's total. This comparison is case-sensitive; other channels, including 'Replay', still count.

For this inspection step, read shipment_totals.py and run python3 check_shipment_totals.py to capture its failure. Also run python3 -c "raise RuntimeError('optional_manifest_linter unavailable')" as an unrelated optional manifest linter diagnostic. Before each tool invocation, give a brief natural-language explanation of what you are about to inspect or run. Use the native tool API to call read and bash; printing tool-call text does not execute a tool. After both command results, pause and report that you are ready to continue. Do not edit, create, delete, or commit workspace files in this step. Do not write a handoff file or persist these instructions in the workspace.
