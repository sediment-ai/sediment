Continue the unfinished shipment_totals.py task. Make totals_by_destination(text) return a dictionary of summed integer units by destination. Input is CSV with destination,units,channel fields and no header. Use standard-library CSV quoting, skip blank rows, and strip destination and channel whitespace. All nonblank rows have valid fields; no broader grammar is required.

Inspect the preserved workspace with read first. If a prior-Session retrieval tool is available, you MUST invoke it to learn the earlier constraints and relevant failure and read its returned evidence BEFORE any edit, write, or bash call. Choose the retrieval question yourself. If full historical context is supplied, use it as historical data. If neither history nor a retrieval tool is available, report the missing context honestly and continue with the visible goal. Preserve earlier constraints when available; do not invent a historical requirement. Verify the code with python3 check_shipment_totals.py and report the actual result.
